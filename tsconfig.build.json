{
  "extends": "./tsconfig.json",
  // Only src is compiled, so the build emits dist/main.js rather than
  // dist/src/main.js. prisma/seed.ts is run through ts-node instead.
  "include": ["src/**/*"],
  "exclude": ["node_modules", "test", "dist", "prisma", "prisma.config.ts", "**/*spec.ts"]
}
